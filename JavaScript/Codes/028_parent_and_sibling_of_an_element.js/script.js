// nextSibling
console.log(document.body.childNodes[0].nextSibling);

// parentNode
console.log(document.body.childNodes[0].parentNode);

// parentElement
console.log(document.body.childNodes[0].parentElement);